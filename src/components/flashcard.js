class Flashcard {
    constructor(flashcard) {
        this.id = flashcard.id 
        this.name = flashcard.name
        this.description = flashcard.description
        this.topic_id = flashcard.topic_id
    }
}